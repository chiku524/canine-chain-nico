# Captured chain inventory

JSON snapshots from `./scripts/capture-chain-inventory.sh` are written here.

Do not commit large or frequently-changing files unless needed for a release audit.  
After capture, copy wasm code IDs and IBC channel summaries into [PHASE0-INVENTORY.md](./PHASE0-INVENTORY.md).
